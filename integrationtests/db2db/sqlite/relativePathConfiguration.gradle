import ch.so.agi.gretl.steps.*

def dbConnString = 'jdbc:sqlite:' + dbsDir + '/db.sqlite'

task relativePathConfiguration(type: Db2DbTask) {
    sourceDb =  [dbConnString, null, null]
    targetDb = [dbConnString, null, null]
    transferSets = [
            new TransferSet('relativePathConfiguration.sql', 'albums_dest', true)
    ];
}