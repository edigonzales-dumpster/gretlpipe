import ch.so.agi.gretl.steps.*

task invalidSrcConnection(type: Db2DbTask) {
    sourceDb =  ['jdbc:sqlite:' + dbsDir + '/dummySource.sqlite',null,null]
    targetDb = ['jdbc:sqlite:' + dbsDir + '/db.sqlite',null,null]
    transferSets = [
            new TransferSet('invalidSrcConnection.sql', 'dummy', true)
    ];
}